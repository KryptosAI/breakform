[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "exl"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Rust",
]

[tool.maturin]
